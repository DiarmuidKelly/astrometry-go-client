# Astrometry Docker Configuration
# Copy this file to .env and adjust paths for your system

# Path to astrometry index files (required)
# This should point to the directory containing index-41XX.fits files
# Download indexes using: ./scripts/download-indexes.sh
ASTROMETRY_INDEX_PATH=./astrometry-data

# Container name (optional, default: astrometry-solver)
# Must match the ContainerName in your ClientConfig when using docker exec mode
CONTAINER_NAME=astrometry-solver
